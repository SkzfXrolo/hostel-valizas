# Copiá a .env.local (local) y definí lo mismo en Vercel → Settings → Environment Variables
# Nunca uses el prefijo VITE_ para estas claves (no deben ir al bundle del cliente).

ADMIN_PASSWORD=elige-una-clave-larga-y-unica
ADMIN_SESSION_SECRET=otra-clave-aleatoria-para-firmar-sesiones
